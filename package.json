{
  "name": "pileup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets:prepare-contact-sheet": "node scripts/prepare-contact-sheet.mjs",
    "assets:build-player": "node scripts/build-player-asset-set.mjs",
    "assets:validate": "node scripts/validate-asset-registry.mjs"
  },
  "dependencies": {
    "phaser": "4.1.0"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  }
}
